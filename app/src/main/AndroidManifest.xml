<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.supermacy.pm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PassManager"
            android:label="@string/title_activity_pass_manager"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".FakePassManager" />
        <activity android:name=".AddItem" />
        <activity android:name=".ViewItem" />
        <activity android:name=".EditItem"></activity>
    </application>

</manifest>
